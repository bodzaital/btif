// =======================
// The configuration file.
// =======================
// entryPoint: the name of the scene (without extensions) that serves as the first page.
// fadeDuration: the amount of time in milliseconds a fade (in or out) lasts. Total duration is twice this amount.
// title: the title of the story (displayed in the title bar).
// debugMode: display runtime exceptions (like CORS blocking, etc).

let conf =
{
	"entryPoint": "scene-0",
	"fadeDuration": 200,
	"title": "btif v0.2",
	"debugMode": true
}